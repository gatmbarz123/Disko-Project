name: Hello World Test

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - amir

jobs:
  hello-world:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Say Hello
        run: echo "Hello, World!"